# @staleflags/core

Core engine for [staleflags](https://github.com/Ijtihed/staleflags) — find dead feature flags and quantify the dead code they create.

This package is used internally by the `staleflags` CLI and GitHub Action. You probably want to install `staleflags` instead.

## License

[MIT](LICENSE)
